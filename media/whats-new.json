{
  "version": "1.42.78",
  "title": "What's New in Kotlin Jump",
  "tagline": "Anonymous implementations counted too",
  "summary": "An implementation written as `object : Interface` was counted only when the line started with a declaration keyword. On a real Android project, 79 of 276 anonymous implementations were invisible, and the Dagger form was one of them.",
  "highlights": [
    {
      "title": "The Dagger form finally counts",
      "description": "`fun provideAdProvider(): AdProvider = object : AdProvider {` left the function branch of the parser before the object on the same line was ever looked at. AdProvider said 1 implementation and had 3.",
      "kind": "fix"
    }
  ],
  "sections": [
    {
      "heading": "Fixes",
      "bullets": [
        "An anonymous implementation is read wherever it sits on the line. It used to be found in two places only, so `return object : Sink {`, `.setListener(object : AnimatorListenerAdapter() {` and the Dagger `= object : X {` were all skipped. 79 of 276 sites on a real project, 23 interfaces whose count was too low.",
        "AudioRepository went from 3 to 5, AdProvider from 1 to 3, AdCallbacks from 3 to 5.",
        "A trailing comment or a string literal that happens to spell `object :` no longer counts as an implementation."
      ]
    }
  ],
  "links": {
    "changelog": "https://github.com/elumine-dev/kotlin-jump/blob/main/CHANGELOG.md",
    "issues": "https://github.com/elumine-dev/kotlin-jump/issues"
  }
}
