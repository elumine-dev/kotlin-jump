<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kotlinjumpdemo">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Audio / Media -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Contacts / Phone -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Notifications / Foreground -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- KJ-023 : aucun code SMS dans le projet → attendu « aucun usage trouvé », grisé. -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- KJ-023 : maxSdkVersion — le badge doit l'afficher sans se tromper de cible. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_pokeball">

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- KJ-023 : la classe GhostActivity N'EXISTE PAS dans le code →
             attendu : entrée grisée « classe introuvable ». -->
        <activity android:name=".GhostActivity" />

        <!-- KJ-023 : référencé UNIQUEMENT par son intent-filter (deeplink) →
             attendu : considéré utilisé, PAS grisé (même si aucun code ne le cite). -->
        <activity android:name=".DeepLinkActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pokedemo" android:host="battle" />
            </intent-filter>
        </activity>

        <!-- KJ-032 fixture: a manifest component is an entry point the code
             never names. The dot is a separator in the harvest, so
             ".ManifestOnlyWorker" yields the token that keeps it alive. -->
        <service android:name=".g5deadweight.ManifestOnlyWorker" />

    </application>

</manifest>
