{
  "version": "1.8.0",
  "title": "What's New in Kotlin Jump",
  "tagline": "Wireless ADB, faster annotations",
  "summary": "Adds wireless ADB connection and pairing from VS Code, fixes device detection reliability, and reduces CPU overhead during editing with incremental annotation scanning.",
  "highlights": [
    {
      "title": "Wireless Android Connection",
      "description": "Connect and pair Android devices over WiFi directly from VS Code using mDNS discovery — no USB cable required after first setup.",
      "kind": "improvement"
    },
    {
      "title": "ADB Reliability Fixes",
      "description": "Device detection now prefers the stable HOST:PORT address format and uses .local mDNS hostnames, eliminating race conditions that caused wireless connections to drop or fail.",
      "kind": "fix"
    },
    {
      "title": "Incremental Annotation Scanning",
      "description": "Hex color and null-assertion providers now scan only the lines that changed instead of the full document on every keystroke, reducing CPU overhead in large files.",
      "kind": "improvement"
    }
  ],
  "sections": [
    {
      "heading": "Improvements",
      "bullets": [
        "Added a Connect via ADB WiFi command that discovers Android devices on the local network using mDNS (dns-sd) and connects wirelessly — removes the need for a USB cable after first setup.",
        "Added a guided Pair via ADB WiFi flow with step-by-step instructions for first-time wireless pairing, so the process works even without prior adb experience.",
        "HexColorFoldingProvider and NullAssertionProvider now perform incremental line scanning — only lines that changed are reprocessed on each edit, cutting CPU overhead in files with many annotations."
      ]
    },
    {
      "heading": "Fixes",
      "bullets": [
        "Fixed a race condition where ADB WiFi connection failed because IP address resolution had not completed — the extension now connects using the stable .local mDNS hostname instead.",
        "Fixed device detection to prefer the HOST:PORT address format and fall back to the adb-XXXX-YYYY serial, preventing misidentified or dropped device connections."
      ]
    }
  ],
  "links": {
    "changelog": "https://github.com/elumine-dev/kotlin-jump/blob/main/CHANGELOG.md",
    "issues": "https://github.com/elumine-dev/kotlin-jump/issues"
  }
}
