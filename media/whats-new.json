{
  "version": "1.42.335",
  "title": "What's New in Kotlin Jump",
  "tagline": "Cross file import cleanup",
  "summary": "This release fixes Remove Everything Unused leaving behind imports, including in Java files, that pointed at a class or dead island it had just deleted, which could break compilation.",
  "highlights": [
    {
      "title": "Stale import cleanup",
      "kind": "fix",
      "description": "Remove Everything Unused now deletes the imports in other files, including Java, that named a class it just removed, so the removal no longer leaves code that fails to compile."
    }
  ],
  "sections": [
    {
      "heading": "Fixes",
      "bullets": [
        "Fixed Remove Everything Unused leaving a Java import pointing at a class it had just deleted. Removing LiveListViewScrollingEvent left LiveDetailFragment.java with import ...LiveListViewScrollingEvent still in place, and javac failed with cannot find symbol, because the workspace wide sweep only reads imports in Kotlin. The command now clears these imports for every removed symbol, the way the single symbol quick fix already did.",
        "Extended the same cleanup to dead island removal, so imports left behind by a removed group of mutually referencing declarations are cleared too instead of staying orphaned in the file."
      ]
    }
  ],
  "links": {
    "changelog": "https://github.com/elumine-dev/kotlin-jump/blob/main/CHANGELOG.md",
    "issues": "https://github.com/elumine-dev/kotlin-jump/issues"
  }
}
