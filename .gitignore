node_modules/
dist/
*.vsix
.vscode-test/
test/kotlin-lsp-main/*
test/vscode-kotlin-main/*
test/kotlin-jump-demo/.gradle/
test/kotlin-jump-demo/build/
scripts/demo/tmp/
scripts/demo/.cache/
scripts/demo/recorder-ext/dist/
scripts/demo/recorder-ext/node_modules/
tmp-demo-e2e/
tmp-demo-frames/
# media/walkthrough/ holds per-demo working dirs (raw.mov + timeline) for
# kjdemo manual-record. The published artefacts (webp + poster + timeline)
# live in media/demos/. The working dir is local-only.
media/walkthrough/

# .publish --dry-run writes a full release preview here so VS Code can
# render the embedded demo webps animated. Overwritten on every dry-run;
# no value in versioning it.
.release-preview.md
.release-preview.whats-new.json

# IntelliJ project files (kotlin-jump-demo fixture)
*.iml
*.ipr
*.iws
.idea/

# Android Gradle local config — must never be checked in
local.properties

# AX harvest dumps (large diagnostic JSON, not versioned)
media/ax-debug-harvest/
