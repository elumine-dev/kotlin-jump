{
  "version": "1.42.127",
  "title": "What's New in Kotlin Jump",
  "tagline": "Version catalogs that are not named libs",
  "summary": "Two halves of version catalog support disagreed whenever the catalog file was not named libs.",
  "highlights": [
    {
      "title": "Renamed Catalogs Are Found",
      "kind": "fix",
      "description": "A catalog such as gradle/deps.versions.toml was never picked up, because the sweep feeding the index looked for that one file name. Any versions.toml under gradle is now indexed."
    },
    {
      "title": "Accessor Root Follows The File Name",
      "kind": "fix",
      "description": "The root was always assumed to be libs, so deps.retrofit in a build file resolved to nothing. Hover and Ctrl+click now take it from the file name, using the same rule as the unused dependency scan."
    }
  ],
  "sections": [
    {
      "heading": "Fixes",
      "bullets": [
        "A catalog named anything other than libs, for example gradle/deps.versions.toml, was never picked up at all: the sweep that feeds the catalog index looked for that one file name. Colouring, folding and navigation inside the file already worked for it, since those are registered on any versions.toml, so half the feature responded and the other half stayed silent.",
        "Even once found, the accessor root was always assumed to be libs. Gradle takes it from the file name, so deps.retrofit in a build file resolved to nothing. Hover and Ctrl+click from a build file now follow the file name, and the rule that already served the unused dependency scan is the one they use, rather than a second copy of it."
      ]
    },
    {
      "heading": "Notes",
      "bullets": [
        "A project whose catalog is the usual gradle/libs.versions.toml sees no change.",
        "No new commands or settings in this release."
      ]
    }
  ],
  "links": {
    "changelog": "https://github.com/elumine-dev/kotlin-jump/blob/main/CHANGELOG.md",
    "issues": "https://github.com/elumine-dev/kotlin-jump/issues"
  }
}
