{
  "version": "1.42.7",
  "title": "What's New in Kotlin Jump",
  "tagline": "Multi-root workspaces done right",
  "summary": "Kotlin Jump 1.42.7 closes the list left by the third display audit: multi-root workspaces that ran tests against the wrong Gradle build and read the wrong version catalog, a Find Usages panel that disagreed with its own lens, a Recent Locations picker that opened a hundred files before showing up, and a few settings and pills that did not do what they said.",
  "highlights": [
    {
      "title": "Tests run against the right Gradle build",
      "kind": "fix",
      "description": "The project root came from the active editor: with two Gradle projects open, a class run from the Test Explorer while the other project had focus ended in \"No tests found\" and every test errored. Each test now uses the project its file belongs to."
    },
    {
      "title": "One version catalog per project",
      "kind": "fix",
      "description": "The last libs.versions.toml read used to win, so a hover in project A showed project B's versions and deleting any toml emptied every hover. The build file's own project answers now."
    },
    {
      "title": "Recent Locations without opening files",
      "kind": "fix",
      "description": "The picker opened up to a hundred documents one by one for their excerpts, each open waking every scanner. Bytes are read sixteen at a time instead, and file names are decoded (Café.kt, not Caf%C3%A9.kt)."
    }
  ],
  "sections": [
    {
      "heading": "Fixes",
      "bullets": [
        "Applies kotlinJump.excludeFromReferences in the Find Usages panel, which used to list files the lens had excluded.",
        "Puts a permission pill on a <uses-permission> tag split over several lines, and makes kotlinJump.semanticHighlighting toggle without a reload.",
        "Removes classes and methods deleted by a checkout from the Test Explorer, and stops flagging @Ignore and @Embedded fields as missing a Room migration."
      ]
    }
  ],
  "links": {
    "changelog": "https://github.com/elumine-dev/kotlin-jump/blob/main/CHANGELOG.md",
    "issues": "https://github.com/elumine-dev/kotlin-jump/issues"
  }
}
