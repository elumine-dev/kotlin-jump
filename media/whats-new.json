{
  "version": "1.42.332",
  "title": "What's New in Kotlin Jump",
  "tagline": "Accurate unused enum entry detection",
  "summary": "This release fixes a dead enum entry detection bug: a @Suppress(\"unused\") annotation on an enum's primary constructor parameter could also silence the unused warning for that enum's first entry, so a truly unused first entry was never flagged or offered for removal. No other extension behavior changed.",
  "highlights": [
    {
      "title": "Constructor Suppress Annotation Fix",
      "description": "Fixes a false suppression that hid a truly unused first enum entry when the enum's primary constructor parameter carried @Suppress(\"unused\").",
      "kind": "fix"
    }
  ],
  "sections": [
    {
      "heading": "Fixes",
      "bullets": [
        "Fixed the unused enum entries detector reading a @Suppress(\"unused\") annotation on the enum's primary constructor parameter as covering the enum's first entry too. On enum class E(@Suppress(\"unused\") val code: Int) { A(1) }, that annotation silenced entry A, so a genuinely unused A was never reported and never offered for removal."
      ]
    },
    {
      "heading": "Notes",
      "bullets": [
        "Ships no other changes to extension behavior: every command and setting works exactly as it did in 1.42.331."
      ]
    }
  ],
  "links": {
    "changelog": "https://github.com/elumine-dev/kotlin-jump/blob/main/CHANGELOG.md",
    "issues": "https://github.com/elumine-dev/kotlin-jump/issues"
  }
}
